# zram swap — critical for 1 GB RAM devices like the HP Stream 7
# Creates a compressed swap device of up to 512 MB using zstd.
[zram0]
zram-size = min(512, ram / 2)
compression-algorithm = zstd
swap-priority = 100
