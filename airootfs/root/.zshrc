# Live-ISO root zshrc — same vaporwave vibe as /etc/skel
source /etc/skel/.zshrc 2>/dev/null || true

# Helpful live-env reminder
echo ""
echo "  Type 'hpstream7-install' to install Arch to the eMMC."
echo ""
